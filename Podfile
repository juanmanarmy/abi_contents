# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'abi_contents' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
	pod 'ModernSearchBar'
  # Pods for abi_contents
	
  target 'abi_contentsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'abi_contentsUITests' do
    # Pods for testing
  end

end
